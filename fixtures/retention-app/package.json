{
  "name": "@accord/retention-app-fixture",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Synthetic legacy retention application used as the investigation target. Owner: Agent 3.",
  "license": "MIT",
  "exports": {
    ".": "./src/index.ts",
    "./resolver": "./src/policy-resolver.ts",
    "./cleanup": "./src/cleanup.ts",
    "./display": "./src/display-policy.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "generate": "node --import tsx scripts/generate.ts",
    "test": "node --import tsx --test test/retention.test.ts"
  },
  "dependencies": {
    "@accord/contracts": "*",
    "@accord/retention-fixture": "*"
  }
}
