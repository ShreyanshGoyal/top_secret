/** fixtures/retention-app — clearly synthetic legacy retention application used as the investigation target.
 * Owner: Agent 3, branch accord/repository. Source IDL, generated policy data, resolver and cleanup path
 * land here. This is fixture code under investigation, never a production dependency of Accord.
 */
export {};
